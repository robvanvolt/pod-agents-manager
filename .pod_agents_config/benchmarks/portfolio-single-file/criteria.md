Score the submitted `index.html` from 0 to 10 using this rubric:

1. Completeness (0–3): hero with name + tagline, about section, at least
   three project cards, skills list, and a contact form are all present.
2. Self-containedness (0–2): a single HTML file with inline CSS/JS only.
   Deduct both points if it references any external URL (CDN, fonts,
   images); deduct one point for minor violations.
3. Responsiveness & theming (0–3): a working dark/light toggle written in
   JavaScript (up to 2 points) and a layout that adapts between mobile and
   desktop widths, e.g. media queries or fluid grid (1 point).
4. Code quality (0–2): valid-looking HTML5 structure, sensible semantics,
   no obviously broken markup or dangling scripts.

A file that is empty, not HTML, or clearly truncated scores 0.
