Create a polished single-file portfolio website for a fictional software
developer named "Alex Doe" and save it as `index.html` in the current
working directory.

Requirements:

- Exactly ONE self-contained HTML file: all CSS and JavaScript inline. No
  external resources of any kind — no CDN links, no web fonts, no image
  files (use CSS, inline SVG, or emoji for visuals).
- Sections: a hero with name and tagline, an about section, at least three
  project cards, a skills list, and a contact form (client-side only, no
  backend).
- Responsive layout that works on both mobile and desktop widths.
- A dark/light theme toggle implemented in JavaScript.
- Valid HTML5 that renders without JavaScript console errors.

Write the complete file to ./index.html, verify it exists, then stop. Do
not create any other files.
