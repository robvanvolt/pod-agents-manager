# pod bench task definition — sourced as KEY="value" lines.
BENCH_DESCRIPTION="Single-file portfolio website in one index.html"
# The file the agent must produce, relative to its (per-run) working directory.
BENCH_ARTIFACT="index.html"
# Per-pod wall-clock budget in seconds (override at run time with POD_BENCH_TIMEOUT).
BENCH_TIMEOUT="900"
